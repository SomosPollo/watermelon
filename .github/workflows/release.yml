name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build darwin/arm64
        env:
          GOOS: darwin
          GOARCH: arm64
        run: go build -ldflags "-X main.Version=${GITHUB_REF_NAME}" -o watermelon-darwin-arm64 ./cmd/watermelon

      - name: Build darwin/amd64
        env:
          GOOS: darwin
          GOARCH: amd64
        run: go build -ldflags "-X main.Version=${GITHUB_REF_NAME}" -o watermelon-darwin-amd64 ./cmd/watermelon

      - name: Build linux/arm64
        env:
          GOOS: linux
          GOARCH: arm64
        run: go build -ldflags "-X main.Version=${GITHUB_REF_NAME}" -o watermelon-linux-arm64 ./cmd/watermelon

      - name: Build linux/amd64
        env:
          GOOS: linux
          GOARCH: amd64
        run: go build -ldflags "-X main.Version=${GITHUB_REF_NAME}" -o watermelon-linux-amd64 ./cmd/watermelon

      - name: Build nfqd linux/arm64
        env:
          GOOS: linux
          GOARCH: arm64
          CGO_ENABLED: 0
        run: go build -ldflags "-X main.Version=${GITHUB_REF_NAME}" -o watermelon-nfqd-linux-arm64 ./cmd/watermelon-nfqd

      - name: Build nfqd linux/amd64
        env:
          GOOS: linux
          GOARCH: amd64
          CGO_ENABLED: 0
        run: go build -ldflags "-X main.Version=${GITHUB_REF_NAME}" -o watermelon-nfqd-linux-amd64 ./cmd/watermelon-nfqd

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            watermelon-darwin-arm64
            watermelon-darwin-amd64
            watermelon-linux-arm64
            watermelon-linux-amd64
            watermelon-nfqd-linux-arm64
            watermelon-nfqd-linux-amd64
